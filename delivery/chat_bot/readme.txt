This folder contains a chat-bot implementation of our model.
We propose two implementation, one for console only (CLI) and one with graphic interface (GUI).

We suggest to use the second since it allows you to chnage model parameters while the chat-bot is running.